{
  "name": "@hiveplane/daemon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck && pnpm test",
    "lint": "pnpm typecheck",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/cli.ts",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@hiveplane/protocol": "workspace:*",
    "zod": "^3.24.1"
  },
  "bin": {
    "hiveplane-bee": "dist/cli.js"
  }
}
