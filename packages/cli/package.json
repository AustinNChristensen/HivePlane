{
  "name": "@hiveplane/cli",
  "version": "0.0.6",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "main": "dist/hive.js",
  "types": "dist/hive.d.ts",
  "bin": {
    "hive": "dist/hive.js",
    "bee": "dist/bee.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck && pnpm test",
    "lint": "pnpm typecheck",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "hive": "tsx src/hive.ts",
    "bee": "tsx src/bee.ts"
  },
  "dependencies": {
    "@hiveplane/daemon": "workspace:*"
  },
  "devDependencies": {}
}
